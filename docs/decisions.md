# Стек и решения (decisions)

Этот документ фиксирует **утверждённый стек** и принятые решения. Изменения вносятся только через обсуждение и редактирование этого файла. Агенты НЕ добавляют зависимости вне секции Approved stack без объяснения «почему» в этом же документе.

## Approved stack

| Area               | Package                                                                                                                                                              | Why                                                                                                                                                                                                                                                                |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Платформа          | React Native `0.86.2` + Expo SDK `~57.0.12`                                                                                                                          | Единая кодовая база для iOS/Android/Web; Expo даёт готовые модули и EAS-билды                                                                                                                                                                                      |
| Язык               | TypeScript `~6.0.3`                                                                                                                                                  | Типобезопасность во всём проекте; типы API генерируются из OpenAPI                                                                                                                                                                                                 |
| Навигация          | `expo-router` `~57.0.12`                                                                                                                                             | Файловая навигация, один источник истины для маршрутов, типизация путей (см. [`architecture.md`](./architecture.md))                                                                                                                                               |
| Состояние          | `@reatom/core`, `@reatom/framework`, `@reatom/npm-react`                                                                                                             | Атомы + асинхронные экшены с `ctx`; единый контекст доступен вне React (см. [`architecture.md`](./architecture.md))                                                                                                                                                |
| Аудио              | `expo-audio` `~57.0.3`                                                                                                                                               | Воспроизведение, экран блокировки, авто-продвижение (native); Web — `HTMLAudioElement` в `.web.ts`                                                                                                                                                                 |
| Изображения        | `expo-image` `~57.0.3`                                                                                                                                               | Персистентный дисковый кэш обложек, переживающий рестарт приложения; нативный lazy-loading по вьюпорту; memory+disk кэш для мгновенного повторного показа; встроенные placeholder/transition. `react-native-fast-image` отклонён — несовместим с New Architecture. |
| Сплеш-скрин        | `expo-splash-screen` `~57.0.8`                                                                                                                                       | Нативный сплеш через config-plugin: с SDK 53+ верхнеуровневое поле splash в app.json игнорируется; без пакета/плагина prebuild генерирует дефолтную заглушку Expo (фикс #54)                                                                                       |
| HTTP-клиент        | `axios` `^1.19.0`                                                                                                                                                    | Интерцепторы для токенов/refresh и мониторинга доступности сервера                                                                                                                                                                                                 |
| Генерация API      | `orval` `8.23.0` + `zod` `^4.4.3`                                                                                                                                    | Клиент, типы и схемы валидации из OpenAPI (`yarn api:generate`)                                                                                                                                                                                                    |
| Парсинг FB2        | `xml-js` `^1.6.11`                                                                                                                                                   | Разбор FB2-книг в объектную модель (см. [`features/book-reader.md`](./features/book-reader.md))                                                                                                                                                                    |
| Хранилище          | `@react-native-async-storage/async-storage` `2.2.0`                                                                                                                  | Токены, позиция, настройки, кэш (см. [`contracts/storage.md`](./contracts/storage.md))                                                                                                                                                                             |
| Анимации/жесты     | `react-native-reanimated` `4.5.1`, `react-native-gesture-handler`, `react-native-worklets`, `react-native-safe-area-context`, `react-native-screens`                 | Стандартный набор для анимаций, жестов и навигации                                                                                                                                                                                                                 |
| Нижняя панель/табы | `@gorhom/bottom-sheet`, `react-native-tab-view`                                                                                                                      | Плейлист/меню в expandable-player; таб-вью на табе «Учиться»                                                                                                                                                                                                       |
| Сеть/офлайн        | `@react-native-community/netinfo` `^12.0.1`                                                                                                                          | Отслеживание подключения, офлайн-баннеры (см. [`features/offline-and-network.md`](./features/offline-and-network.md))                                                                                                                                              |
| Обновления         | `expo-notifications` `~57.0.10`, `expo-task-manager`, `react-native-zip-archive` `^9.4.0`, локальный модуль `modules/apk-installer` (`file:./modules/apk-installer`) | Уведомления о новых версиях и самообновление: распаковка ZIP-ассета релиза с APK; установка через Android `PackageInstaller` (см. [`features/updates.md`](./features/updates.md))                                                                                  |
| UI-базовое         | `@expo/vector-icons`, `expo-blur`, `expo-linear-gradient`, `expo-status-bar`                                                                                         | Иконки, blur-таббар, градиенты, статус-бар                                                                                                                                                                                                                         |
| QR-код             | `react-native-qrcode-svg` `^6.3.21`, `react-native-svg` `15.15.4`                                                                                                    | Экран «Поделиться приложением»: QR-код со ссылкой на релиз; `react-native-svg` — нативная зависимость `react-native-qrcode-svg` (ставится через `expo install`)                                                                                                    |
| Тесты              | `jest` + `jest-expo` + `@testing-library/react-native`                                                                                                               | Юнит/компонентные тесты рядом с кодом                                                                                                                                                                                                                              |
| Моки API           | `msw` `^2.15.0`                                                                                                                                                      | Разработка/тесты без живого бэкенда                                                                                                                                                                                                                                |
| Прочее             | `debounce`, `ts-pattern`, `react-native-text-ticker`                                                                                                                 | Утилиты и паттерн-матчинг                                                                                                                                                                                                                                          |
| Патчи зависимостей | `patch-package` `^8.0.1`                                                                                                                                             | Инфраструктура патчей сохранена (no-op с нулём патчей, `postinstall` в т.ч. в EAS-сборках). Патч gorhom 5.2.14 **удалён** (Issue #69) — см. ниже.                                                                                                                       |

> **Почему `patch-package` (история):** gorhom 5.2.14 содержал sub-pixel-баг в derived-статусе шторки: состояние EXTENDED определялось точным равенством `animatedPosition.value === containerHeight - animatedSheetHeight.value` (`BottomSheet.tsx` ~L308). При дробном PixelRatio позиция пружины и измеренная высота расходятся на доли пикселя → `===` никогда не истинно → вложенный скроллящийся список остаётся LOCKED навсегда (`useScrollable.ts:41-76`), откатывая каждый программный скролл в 0 (линия issues #2737; master == 5.2.14, фикса нет). Патч заменял точное равенство на epsilon-сравнение `Math.abs(...) < 0.5` в трёх местах (EXTENDED, EXTENDED-with-keyboard, FILL_PARENT) — `patches/@gorhom+bottom-sheet+5.2.14.patch`. Scope: один патч, три копии файла (`src/`, `lib/commonjs/`, `lib/module/`). `postinstall: patch-package` применял патч при каждой установке, включая EAS-сборки.

> **Патч gorhom УДАЛЁН (Issue #69):** шторка плейлиста теперь работает с `enableContentPanningGesture={false}` ⇒ gorhom's scrollable-unlock безусловно разблокирует вложенный список (short-circuit в UNLOCKED) ⇒ epsilon-патч #48 потерял поведенческого потребителя. Проверено grep'ом: `PlaylistBottomSheet` — единственный потребитель `@gorhom/bottom-sheet` в `src/`. Патч удалён, версия в package.json не менялась (^5.2.14, resolved 5.2.14), node_modules возвращён к upstream (`===`-сравнения восстановлены). Инфраструктура `patch-package` (postinstall + devDependency) **сохранена** — no-op с нулём патчей, оставлена для будущего использования. **Caveat:** если content-panning когда-либо снова включат или добавят второй `@gorhom/bottom-sheet`-шит, патч #48 (sub-pixel EXTENDED lock) необходимо восстановить — он восстанавливается из git-истории (`git show <commit>:patches/@gorhom+bottom-sheet+5.2.14.patch`).

> ⚠️ **Gluestack UI не используется** — в `package.json` отсутствует. **expo-system-ui отсутствует**; Material You реализован кастомно через `src/shared/ui/theme/materialYou.ts` (Android 12+, API 31) и динамические цвета таббара в `src/widgets/tab-bar/ui/CustomTabBar.tsx` (`Color.android.dynamic.primaryContainer`). Подробности — в [`features/theme.md`](./features/theme.md).

> **Почему локальный модуль `modules/apk-installer`:** для self-update через `PackageInstaller` на RN 0.86 / Expo SDK 57 нет поддерживаемого npm-пакета (существующие RN-библиотеки устарели/несовместимы с New Architecture). Локальный Expo-модуль (`file:`-зависимость) не добавляет новых npm-зависимостей, автолинкуется через `expo-modules-autolinking` и собирается в CI `prebuild --clean` автоматически. Причина замены `ACTION_VIEW`: системный установщик (Android 16) удалял APK до чтения (`FileNotFoundException` в `InstallRepository.stageForInstall`), т.к. `startActivityAsync` резолвится при старте интента, а не по завершении установки.

## Rejected (and why)

| Proposal                                   | Verdict   | Why                                                                                                                                                                                             |
| ------------------------------------------ | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Zustand                                    | Отклонено | Проект уже использует Reatom; Zustand не дал бы преимуществ, а добавил бы вторую модель состояния. <!-- TODO: уточнить у команды, был ли Zustand реально рассмотрен или выбран сразу Reatom --> |
| Redux Toolkit                              | Отклонено | Избыточен для текущего размера приложения; Reatom проще (атомы против редьюсеров/селекторов).                                                                                                   |
| Навигация через `react-navigation` вручную | Отклонено | Выбран expo-router (файловая навигация) — см. Approved stack. `react-navigation` всё равно лежит в основе expo-router, но конфиг не пишем вручную.                                              |
| `vite-plugin-pwa` для web-PWA               | Отклонено | Проект собирает web через Metro (не Vite). Плагина-аналога для Expo Metro нет. Service Worker написан руками (`public/sw.js`), без зависимостей. Workbox как пост-билд шаг — вариант на будущее (см. [`features/web.md`](./features/web.md)).                                                                                                                              |
| `axios` для скачивания аудио в кеш на web   | Отклонено | Аудио-хост без CORS; нужен `fetch(url, { mode: 'no-cors' })` → opaque Response, чего XMLHttpRequest (и поверх него axios) не умеет. `webAudioDownload.ts` остаётся на `fetch`.                                                                                                                                                                                          |

## Other fixed decisions

- **Архитектура:** Feature-Sliced Design (слои app → pages → widgets → features → entities → shared) — см. [`architecture.md`](./architecture.md).
- **Стиль:** отсутствие точек с запятой; одинарные кавычки; до 130 строк на файл; стрелочные функции.
- **Экспорты:** именованные экспорты в `src/`, `export default` только в `app/` (expo-router).
- **Импорты:** абсолютные по слоям FSD (`entities/player`, `shared/ui/theme`); относительные между слоями запрещены.
- **Barrel:** один barrel `index.ts` на слайс; сегментные barrel-файлы запрещены; именованные реэкспорты вместо `export *`; экспортируется только то, что используется внешними потребителями.
- **@x cross-import:** кросс-слойные связи между слаями одного уровня (например, `entities/player` ↔ `entities/listening-history`) реализуются через папку `@x/` в корне слайса-поставщика. Файл `@x/<consumer>.ts` реэкспортирует узкий API для конкретного потребителя. Подробнее — [`architecture.md`](./architecture.md) → «@x cross-import».
- **Состояние:** Reatom; `ctx` из `src/shared/lib/reatom-ctx/ctx.ts`.
- **Платформы:** iOS, Android, Web; платформенные реализации через `.native.ts`/`.web.ts`.
- **Web-сборка:** Metro, `web.output` не задан (SPA-режим `single`: один `index.html` + клиентский роутинг). PWA-оболочка — `public/index.html` + `public/sw.js` (hand-rolled Service Worker: офлайн-оболочка + офлайн-аудио через Cache Storage). Dev-web запускается через `yarn web` (`EXPO_NO_METRO_LAZY=1` — обход бага ленивого бандлинга Metro на `import('expo-notifications')`). Деплой-хост обязан отдавать `index.html` на неизвестные пути — закрыто `nginx.conf` (`try_files`) в собственном Docker-образе (`Dockerfile`). Подробнее — [`features/web.md`](./features/web.md).
- **Web-деплой:** отдельное Docker-приложение `slovo-web` на общем VPS, по паттерну `slovo-frontend`/`slovo-docs` из `slovo-propovedi-playbook` (инфра — Traefik/Docker/buildx — от плейбука; сам деплой — Forgejo Actions по тегу `v*`, `scripts/vps-deploy-web.sh`, без registry). `release.yml`: job `web` (экспорт + деплой) и job `android` (APK) оба зависят только от `create-release`, друг от друга — нет.
- **Язык UI:** русский; технические термины — английским.
- **API:** контракт через OpenAPI + Orval; ручные типы под API не пишем.
- **Android build flavors:** Gradle `productFlavors` `dev`/`prod` (dimension `env`). Dev: `applicationIdSuffix ".dev"`, имя «Dev Слово.Проповеди». Prod: базовый `ru.slovopropovedi`. `debuggableVariants = ["devDebug", "prodDebug"]`. Цель — dev-сборка не перезаписывает prod на одном устройстве. См. [`BUILD-LOCAL.md`](./BUILD-LOCAL.md).
- **CI: ограничение памяти Jest.** В `jest.config.ts` добавлен `workerIdleMemoryLimit: '1GB'` (перезапуск воркера при росте RSS), а в `.forgejo/workflows/ci.yml` шаг тестов запускается как `yarn testFinal --maxWorkers=2`. Причина: тяжёлые `jest-expo`/RN UI-сьюты вызывали OOM (SIGKILL от ОС) при параллельном запуске множества воркеров.
- **CI: OOM релизной сборки (exit 137) на 4 ГБ раннере.** На хосте раннера ≤4 ГБ RAM и два слота, поэтому тяжёлые задачи не должны пересекаться. Принятые меры: `kotlin.compiler.execution.strategy=in-process` в `android/gradle.properties` (прописывается после prebuild в release.yml) — дефолтная стратегия порождала отдельный Kotlin-daemon, который жил во время JS-бандла и съедал память; `org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m (в раунде 2 повышено до 768m — см. ниже)`; `NODE_OPTIONS=--max-old-space-size=1536` для node/Metro; `metro.config.js` читает `CI_METRO_MAX_WORKERS` и ограничивает transform-воркеры Metro двумя только в CI (локально поведение не меняется); concurrency-группа `memory-heavy-jobs` в ci.yml (`checks`) и release.yml (`release`) — тяжёлые задачи выстраиваются в очередь вместо параллельного запуска. Страховкой для пиков служит swap на хосте (добавлен вручную, вне репозитория). **Раунд 2 (нативная фаза):** после того как JS-бандл прошёл, daemon умирал без исключения в логе (kernel/cgroup OOM-kill) во время clang/CMake-фазы для двух ABI, несмотря на 14 ГБ swap на хосте — подозрение на лимит памяти контейнера. Дополнительные меры: `-XX:MaxMetaspaceSize=768m` (in-process Kotlin поднимает использование metaspace; 768m — запас, metaspace коммитит только реально используемое); `org.gradle.workers.max=2` (ограничение параллельных compile-задач, чтобы сгладить пики памяти); сборка только под `arm64-v8a` (отказ от 32-битного `armeabi-v7a` — почти все устройства с ~2018 arm64; вдвое снижает пик памяти/времени нативной компиляции). **Раунд 3 (диагноз окончательный):** прогон с правками раунда-2 упал через 29 мин в `:app:buildCMakeRelWithDebInfo[arm64-v8a]` — `Gradle build daemon disappeared unexpectedly` без исключения в логе демона. dmesg хоста показал оба kill'а как `constraint=CONSTRAINT_MEMCG` (лимит job-контейнера 6 GiB, а не хоста); убит java-демон с anon-rss всего ~1.7–2.0 GiB — как крупнейший процесс в cgroup, пока 6 GiB забивали некапнутые clang-процессы (ninja -j выводится из числа ядер CPU). По исходникам react-native-gradle-plugin подтверждено: `org.gradle.workers.max` НЕ ограничивает ninja -j внутри одного CMake-таска — только параллельные Gradle-таски; комментарии в release.yml об этом исправлены. Хостовой swap (10 GiB свободно) не спасает: у job-контейнера swap-квота не позволяет выходить за memory.max. **Решение:** поднять лимит job-контейнера в конфиге act-runner (`--memory=12g --memory-swap=16g`); значения в workflow (Xmx1536, metaspace 768m, NODE_OPTIONS 1536, workers.max 2, arm64-v8a) оставлены как консервативный пол. **План Б** (если OOM повторится на 12g): инжект `CMAKE_JOB_POOLS` (`compile=N; link=1`) постпребилд-патчем `app/build.gradle`.

## Superseded (отменённые решения)

Секция для устаревших решений. Отменённое решение помечается зачёркиванием с объяснением, **не удаляется**:

~~**Пример:** Redux Toolkit — ранее рассматривался для управления состоянием. Заменён на Reatom (см. выше) — атомы проще и не требуют boilerplate.~~

Сейчас других отменённых решений нет.

## Связанные документы

- [`architecture.md`](./architecture.md) — обоснование ключевых решений.
- [`conventions.md`](./conventions.md) — правила добавления зависимостей и DoD.
- [`contracts/rest-api.md`](./contracts/rest-api.md) — детали API-клиента.
