/** Expand animation duration — shared so consumers can time mount/unmount against it. */
export const EXPAND_DURATION_MS = 300
/** Collapse animation duration — shared so consumers can time mount/unmount against it. */
export const COLLAPSE_DURATION_MS = 250
